name: New implementation or service
description: Tell us about a Parley implementation (Go, Rust, …) or a service you built
labels: [ecosystem, status/triage]
body:
  - type: input
    id: link
    attributes: { label: Link }
    validations: { required: true }
  - type: textarea
    id: about
    attributes: { label: About it, description: "Language/service, conformance status (all vectors passing?), interop tested against which implementation?" }
