<!-- Thanks for contributing to Parley! -->

**What and why**

**Checklist**
- [ ] `npm test` passes (and `cd python && uv run pytest` if protocol behavior changed)
- [ ] If this changes bytes on the wire or Lens output: SPEC.md is updated and vectors are regenerated (`cd ts && node scripts/vectors.mjs`)
